import os

from src.gameloop import command_loop

# This is eventually where the primary calls will go to start the game.

# Adding this here to start the game, but this will be called elsewhere.
command_loop()